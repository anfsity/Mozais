name: mozais_greeter_components
description: Reusable visual components for compiled Mozais greeter themes.
publish_to: none

environment:
  sdk: ^3.13.2

dependencies:
  flutter:
    sdk: flutter
  mozais_theme_sdk:
    path: ../mozais_theme_sdk

dev_dependencies:
  flutter_lints: ^6.0.0
