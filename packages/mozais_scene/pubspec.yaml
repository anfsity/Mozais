name: mozais_scene
description: Compile-time scene model and runtime for the Mozais greeter.
publish_to: none

environment:
  sdk: ^3.13.2

dependencies:
  flutter:
    sdk: flutter
  mozais_scene_schema:
    path: ../mozais_scene_schema

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
