name: mozais_theme_sdk
description: Compile-time Flutter theme API for Mozais greeters.
publish_to: none

environment:
  sdk: ^3.13.2

dependencies:
  flutter:
    sdk: flutter
  mozais_scene:
    path: ../mozais_scene

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
