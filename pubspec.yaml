name: mozais_greeter
description: Compile-time themeable Flutter greeter for Mozais.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.13.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  dbus: ^0.7.11
  mozais_greeter_ui:
    path: packages/mozais_greeter_ui
  mozais_scene:
    path: packages/mozais_scene

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/
